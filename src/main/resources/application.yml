# Server configuration
server:
  port: 9191

# Database connection & configuration
spring:
  datasource:
    username: postgres
    password: Gios#5663
    url: jdbc:postgresql://localhost:5432/ecommerce-backend
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
    open-in-view: false
  thymeleaf:
    prefix:
    check-template-location: false
  # SMTP configuration
  mail:
    host: localhost
    port: 25

#    username:
#    password:
#    properties:
#      mail:
#        smtp:
#          auth: true
#          starttls:
#            enable: true



# Encryption configuration
encryption:
  salt:
    rounds: 10

# JWT configuration
jwt:
  algorithm:
    key: SuperSecureSecretKey
  issuer: eCommerce
  expiryInSeconds: 604800 # a week

  verification:
    expiryInSeconds: 1800 # half an hour

# email configuration
email:
  from: no-reply@ecommerce.com

# General configuration
app:
  frontend:
    url: http://ecommerce.com


